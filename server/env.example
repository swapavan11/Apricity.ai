NODE_ENV=development

# URLs - CRITICAL: Update these for production
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:5000

# CORS - For production, add all allowed origins (comma-separated)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# Leave empty to use FRONTEND_URL only
ALLOWED_ORIGINS=

# Cookie Domain - Only needed if using subdomains
# Example: COOKIE_DOMAIN=.yourdomain.com
# Leave empty for default behavior
COOKIE_DOMAIN=

# Gemini (Google Generative AI)
# Get an API key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key
# Optional: override model (defaults to gemini-1.5-flash). Examples:
# gemini-2.5-flash | gemini-2.0-flash | gemini-1.5-flash | gemini-1.5-pro
GEMINI_MODEL=gemini-1.5-flash

# MongoDB
# For production, use MongoDB Atlas: mongodb+srv://user:pass@cluster.mongodb.net/dbname
MONGODB_URI=mongodb://localhost:27017/Apricity

# JWT - MUST be strong in production (32+ random characters)
JWT_SECRET=change-me-super-secret-min-32-characters
JWT_EXPIRES_IN=7d

# Session - MUST be strong in production (32+ random characters)
SESSION_SECRET=change-me-session-secret-min-32-characters

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# For production, use: https://your-backend-domain.com/api/auth/google/callback
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# Email (Nodemailer) - Required for email verification and password reset
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_FROM=noreply@yourdomain.com

# Cloudinary - For file uploads (PDFs, avatars)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# SMS/OTP removed: no Twilio configuration required
